# 봉사모아

사설·개인 봉사센터와 봉사자를 연결하는 서비스. **웹 먼저, 앱은 나중.**

## 지금 상태

**`docs/05_개발순서.md` 0~3단계 완료.** 배포(https://bongsamoa.vercel.app), Supabase DB·RLS·신청 함수, 이메일 회원가입·로그인까지 실제로 동작합니다.
**다음은 4단계(센터 관리 화면)** 부터입니다. 자세한 진행 상황·코드 구조·겪은 문제는 `docs/00_현재상태.md`의 "지금 코드 상태" 절을 먼저 읽으세요.
**특히 그 안의 "작업 원칙" 절** — 화면을 만들 때마다 실제 사용자 흐름을 UI/UX 관점에서 먼저 점검하라는 지침이니 빠뜨리지 마세요.

## 문서 (작업 전 반드시 읽을 것)

| 파일 | 언제 보나 |
|---|---|
| `docs/00_현재상태.md` | **여기부터.** 결정 19가지와 변경 이력 |
| `docs/05_개발순서.md` | **지금 무엇을 할지.** 8단계 |
| `docs/02_데이터설계.md` | DB 만들 때. 테이블 10개 + 반드시 처리할 규칙 7가지 |
| `docs/01_기획서.md` | 화면을 만들 때. 왜 이렇게 정했는지 |
| `docs/03_웹에서_앱으로.md` | URL 구조 (한 번 정하면 안 바꿈) |
| `봉사모아_화면시안.html` | **눌러볼 수 있는 화면 30개.** 브라우저로 열 것 |

## 함께 일하는 사람

**웹 개발이 처음입니다.** 봉사 모임의 모임장이고, 자기 모임을 위해 만들고 있습니다.

- 코드 문법은 몰라도 됩니다. **무엇이 왜 바뀌는지**를 설명해 주세요
- 터미널 명령은 **뭘 치는지, 왜 치는지** 함께 알려주세요
- 한 번에 화면 하나씩. «센터 관리 다 만들어줘» 같은 요청이 와도 나눠서 진행하세요

## 기술

Next.js (App Router) · Supabase (DB + Auth) · Vercel · TypeScript

## 절대 어기면 안 되는 것

`docs/02_데이터설계.md` 3장에 자세히 있습니다. 요약하면 —

1. **정원 초과** — 신청은 반드시 **DB 함수 안에서** 잠그고 처리. 화면 코드로 세면 동시 신청에 뚫립니다
2. **RLS** — 모든 테이블에 켜고 시작. 나중에 켜면 뭘 열어뒀는지 모릅니다
3. **회차는 미리 만들어 저장** — 규칙에서 매번 계산하지 않습니다. 예외 처리가 안 됩니다
4. **회차 휴무 시 신청자 전원 자동 취소** — 안 하면 유령 봉사가 남습니다
5. **닉네임 unique** — 이름을 가려 보여주므로 닉네임이 유일해야 사람을 구분합니다
6. **같은 사실을 두 곳에 저장하지 않기**

## 개인정보 — 이 서비스의 성격을 정합니다

받는 것은 **이메일 · 이름 · 닉네임 · 프로필 사진(선택)** 뿐입니다.

- **전화번호를 받지 않습니다**
- **이름은 어디서든 `김*연` 으로 가려서** 보여줍니다. 화면에 나가는 이름은 닉네임
- 센터 대표 연락처는 **센터 정보에서 공개**하되, 목록 카드에는 넣지 않습니다

## 보험과 책임 범위

봉사모아에 올라오는 곳은 **1365에 등록되지 않은 개인·소규모 센터**입니다. 봉사시간 실적도, 봉사자 보험도 대부분 없습니다.

```
봉사모아  →  자리를 연결한다
센터      →  활동을 진행한다. 현장 안전을 맡는다
봉사자    →  조건을 확인하고 참여한다
```

이 구분을 화면 여러 곳에 **같은 말로** 적습니다. 신청 팝업의 동의 체크는 `applications.agreed_at` 에 시각을 남깁니다.

**겁주지 않습니다.** 빨간 경고 대신 차분한 회청색, 반복 대신 결정 시점에 한 번, 「책임지지 않습니다」 대신 「센터와 함께 해결합니다」.

## 시안을 코드로 옮길 때

시안에서 화면 두 개로 그린 것이 **개발에서도 두 개면 안 됩니다.** 시안 HTML에 `⚠ 개발 지침` 주석으로 표시해 뒀습니다.

| 시안 | 코드 |
|---|---|
| 봉사 찾기 · 센터 전체 | `<CenterList limit={4} />` / `<CenterList />` |
| 다가오는 회차 명단 · 지난 회차 명단 | 한 화면 + `session_date` 비교 |
| 다가오는 봉사 상세 · 지난 봉사 상세 | 한 화면 + 상태 |

**화면 문구는 한 파일에 모으세요.** 시안에서 「후기 → 이야기」를 바꿀 때 60군데를 고쳤습니다. 실제 앱에서는 그런 일이 없어야 합니다.

## 성공 기준

가입자 수가 아닙니다.

> **실제 사설 봉사센터 1곳이, 다음 달 봉사 일정을 엑셀이 아니라 봉사모아로 운영하는 것.**

빨리 6단계(실제 운영)까지 가는 것이 전부입니다. 완벽하게 만들려다 공개를 못 하는 것이 가장 큰 위험입니다.
